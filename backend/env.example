# ============================================================================
# LLM Provider Configuration
# ============================================================================
# Select which LLM provider to use
# Options: "azure_openai" or "google_gemini"
# Default: "azure_openai"
LLM_PROVIDER=azure_openai

# ============================================================================
# Azure OpenAI Configuration
# ============================================================================
# Required when LLM_PROVIDER=azure_openai
# Get these from your Azure OpenAI resource in Azure Portal
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_API_VERSION=2024-10-21
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# ============================================================================
# Google Gemini Configuration
# ============================================================================
# Required when LLM_PROVIDER=google_gemini
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=your-google-gemini-api-key-here
GOOGLE_GEMINI_MODEL=gemini-1.5-pro

# ============================================================================
# Face Image Service Configuration (Optional)
# ============================================================================
# Set to "true" to use mock photos from backend/mock-data/photos/
# Set to "false" to use Google Cloud Storage (requires GCS credentials)
USE_MOCK_DATA=true

# Google Cloud Storage configuration (only used when USE_MOCK_DATA=false)
GCS_BUCKET_NAME=pf_ai_app
GCS_PHOTOS_PATH=photos
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcs-service-account.json

# ============================================================================
# Usage Instructions
# ============================================================================
# 1. Copy this file to .env: cp env.example .env
# 2. Set LLM_PROVIDER to your preferred provider (azure_openai or google_gemini)
# 3. Fill in the corresponding API keys and configuration
# 4. For production photo storage, configure GCS settings
# 5. Restart the application
#
# Example for Azure OpenAI:
#   LLM_PROVIDER=azure_openai
#   AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
#   AZURE_OPENAI_API_KEY=your-key-here
#
# Example for Google Gemini:
#   LLM_PROVIDER=google_gemini
#   GOOGLE_GEMINI_API_KEY=your-key-here
#   GOOGLE_GEMINI_MODEL=gemini-1.5-pro
